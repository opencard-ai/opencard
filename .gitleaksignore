# Gitleaks fingerprint allowlist — historical leaks already remediated.
#
# Format: <commit_sha>:<file>:<rule_id>:<line>
#
# Empty as of 2026-05-07: the 5 historical leaks (Upstash KV token x2 +
# MiniMax API key x3) were scrubbed from git history via git-filter-repo,
# not just allowlisted. The original commit SHAs no longer exist, so
# fingerprints would be meaningless.
#
# DO NOT add live, un-rotated leaks here. Rotate first, then either scrub
# from history (preferred) or allowlist.
